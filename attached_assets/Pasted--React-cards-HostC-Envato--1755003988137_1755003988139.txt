Создай компонент React — секцию карточек (cards) в стиле шаблона HostC / Envato: современные карточки-продукты/услуг с иконкой/изображением, заголовком, описанием, списком преимуществ, ценой/лейблом и кнопкой CTA. Это будет использоваться в лендинге школы FGS для разделов «Кружки», «Факты о школе» или похожих блоков.

Требования к реализации:

Технологии: React functional component, Tailwind CSS для стилей. Укажи альтернативу (plain CSS) и пример импорта, если пользователь не использует Tailwind.

Адаптивность: сетка 3-в-ряд на десктопе, 2-в-ряд на планшете, 1-в-ряд на мобильных.

Карточка должна содержать:

Квадратную/круглую иконку или мини-изображение сверху.

Заголовок (title).

Короткое описание (1–2 строки).

Список 3–4 фич/пунктов (иконка + текст).

Лейбл/тэг (например: «Популярно», «Новый» или цена).

CTA-кнопку (например: «Подробнее» или «Записаться»), которая ведёт на передаваемый URL.

Внешний вид & поведение:

Карточка с аккуратной тенью, скруглёнными углами и легким lift-эффектом при hover (transform: translateY(-6px) + stronger shadow).

При hover — плавное изменение градиента/фонового цвета заголовка или тени (transition 200–300ms).

Поддержка dark mode (простой вариант: переключение классов).

Появление (fade-in + slight-up) при скролле (используй Intersection Observer; fallback — анимация при mount).

Редактируемость:

Данные карточек передаются через пропс items — массив объектов { id, iconUrl, title, description, features: [], tag, ctaText, ctaUrl }.

Приведи пример items с 6 карточками и заполненными данными (текстами на казахском/русском для школы FGS).

Доступность (a11y): кнопки с aria-label, семантическая разметка (article/section), изображения с alt.

Оптимизация: lazy loading для картинок (loading="lazy").

Готовность к интеграции: экспорт по умолчанию компонента SchoolCards, короткая инструкция как импортировать и вставить (пример в App.jsx).

Дополнительно: создай опциональный проп showAllButton (boolean). Если true, рендерить под сеткой большую кнопку «Посмотреть все кружки» (ведёт на /kruzhki).

Комментарии в коде: краткие пояснения, где менять цвета, шрифты, иконки и тексты.

Ожидаемый результат: файл SchoolCards.jsx (или SchoolCards.tsx), пример использования в App.jsx, пример данных items, и минимальный блок Tailwind-классов (или CSS) для копирования в проект.